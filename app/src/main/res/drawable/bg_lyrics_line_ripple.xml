<?xml version="1.0" encoding="utf-8"?>
<!-- Mirrors Apple's bg_lyrics_line_ripple.xml. The ripple color is Apple's
     player_vibrant_quarternary (8% white) — defined inline here since we don't
     yet carry the full vibrant palette. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#14FFFFFF">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/black" />
            <corners android:radius="@dimen/lyrics_ripple_corner_radius" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/lyrics_ripple_corner_radius" />
        </shape>
    </item>
</ripple>
